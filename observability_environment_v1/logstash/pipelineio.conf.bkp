input {
    # example: using file to input data
    #file {
    #    path => "/file-path/file-name.extension"
    #}

    tcp {
        port => 5000
    }

    http {

    }
}

#filter {
    # example: using csv plugin
    #csv {
    #    columns => ["city", "state", "country"]
    #}
#}

output {
    elasticsearch {
        hosts => "${ELASTICSEARCH_HOST}"
        user => "${ELASTIC_USERNAME}"
        password => "${ELASTIC_PASSWORD}"
        index => "tcp-elk_prometheus_grafana_zipkin_mysql_v1_demo"
    }

    elasticsearch {
        hosts => "${ELASTICSEARCH_HOST}"
        user => "${ELASTIC_USERNAME}"
        password => "${ELASTIC_PASSWORD}"
        index => "http-elk_prometheus_grafana_zipkin_mysql_v1_demo"
    }

    elasticsearch {
        hosts => "${ELASTICSEARCH_HOST}"
        user => "${ELASTIC_USERNAME}"
        password => "${ELASTIC_PASSWORD}"
        index => "postman-elk_prometheus_grafana_zipkin_mysql_v1_demo"k
    }
}
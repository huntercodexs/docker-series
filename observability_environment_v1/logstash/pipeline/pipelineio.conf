input {
  udp {
    port => 5000
    codec => plain { charset => "UTF-8" }
    type => "syslog"
  }
}

filter {
}

output {
  stdout { codec => rubydebug }

  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "${ELASTIC_USERNAME}"
    password => "${ELASTIC_PASSWORD}"
    index => "otel-logstash-v1-%{+YYYY.MM.dd}"
    ssl => false
  }
}
